<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.common.mapper.CommonCodeMapper">

    <select id="findCommonCodeSqByName" parameterType="map" resultType="Long">
    SELECT common_code_sq
    FROM TBL_COMMON_CODE_C
    WHERE COMMON_CODE_NM = #{name} AND PARENT_COMMON_CODE_SQ = #{parentCodeSq}
	</select>
	
	<select id="findCommonCodeSqByEngName" parameterType="map" resultType="Long">
	SELECT common_code_sq
	FROM TBL_COMMON_CODE_C
	WHERE COMMON_CEDE_ENGLISH_NM = #{engName} AND PARENT_COMMON_CODE_SQ = #{parentCodeSq}
	</select>
	
	<select id="findSkillTagSqByName" parameterType="String" resultType="Long">
	SELECT skill_tag_sq
	FROM TBL_SKILL_TAG_C
	WHERE SKILL_TAG_NM = #{name}
	</select>
	
	<select id="findSkillTagInfoByName" parameterType="String" resultType="com.example.demo.domain.project.dto.request.SkillInsertRequest">
    SELECT
        skill_tag_sq AS skillTagSq,
        parent_skill_tag_sq AS parentSkillTagSq,
        skill_tag_lvl AS skillTagLvl,
        skill_tag_nm AS skillTagNm
    FROM TBL_SKILL_TAG_C
    WHERE skill_tag_nm = #{name}
	</select>
	
	<select id="findParentSkillTagSqByName" parameterType="String" resultType="Long">
	SELECT skill_tag_sq
	FROM TBL_SKILL_TAG_C
	WHERE SKILL_TAG_SQ = #{skill_tag_sq}
	</select>

</mapper>