<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.domain.user.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.example.demo.domain.user.dto.request.UserJoinRequestDto">
        INSERT INTO TBL_USER_M (
            user_id,
            user_email,
            user_pw,
            user_nm,
            user_gender_cd,
            user_phone_num,
            user_birth_dt,
            user_type_cd,
            user_signup_type_cd,
            user_agreed_privacy_policy_yn
        ) VALUES (
            #{userId},
            #{userEmail},
            #{userPw},
            #{userNm},
            #{userGenderCd},
            #{userPhoneNum},
            #{userBirthDt},
            #{userTypeCd},
            #{userSignupTypeCd},
            #{userAgreedPrivacyPolicyYn}
        )
    </insert>

    <select id="countByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM TBL_USER_M WHERE user_email = #{userEmail}
    </select>

    <select id="findByEmail" parameterType="string" resultType="com.example.demo.domain.user.dto.response.UserJoinResponseDto">
        SELECT
            user_sq AS userSq,
            user_email AS userEmail,
            user_nm AS userNm,
            user_created_at_dtm AS userCreatedAtDtm
        FROM TBL_USER_M
        WHERE user_email = #{userEmail}
    </select>

</mapper>
